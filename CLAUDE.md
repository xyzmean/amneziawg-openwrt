# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Обзор проекта

YAAWG (Yet another AmneziaWG variation for OpenWrt) — это пакет AmneziaWG VPN для OpenWrt. Репозиторий синхронизируется с четырьмя апстрим-проектами:
- amneziawg-tools (инструменты userspace)
- amneziawg-linux-kernel-module (kernel module)
- amneziawg-go (userspace реализация на Go)
- luci-proto-wireguard (Web UI для OpenWrt LuCI)

Проект поддерживает протокол AmneziaWG v2.0, включая параметры S3-S4, I1-I5 и ranged H1-H4.

## Структура репозитория

```
amneziawg-openwrt/
├── amneziawg-tools/          # Userspace инструменты и netifd protocol helper
├── amneziawg-go/             # Userspace реализация на Go (альтернатива kernel module)
├── kmod-amneziawg/           # Kernel module для AmneziaWG
├── luci-proto-amneziawg/     # Web интерфейс для LuCI
└── .github/workflows/        # GitHub Actions для сборки пакетов
```

### Архитектура пакетов

1. **amneziawg-tools**: Содержит `awg` (CLI инструмент), netifd protocol helper (`amneziawg.sh`) и watchdog-скрипт. Скрипты находятся в `files/`.

2. **amneziawg-go**: Userspace реализация протокола на Go. Требует Go > 1.24.4. Используется как fallback, если kernel module недоступен.

3. **kmod-amneziawg**: Kernel module — рекомендуемый вариант по умолчанию. Требует меньше ресурсов и обеспечивает более высокую производительность.

4. **luci-proto-amneziawg**: Web UI для управления AmneziaWG через LuCI. Основной JavaScript код в `htdocs/luci-static/resources/protocol/amneziawg.js`. Конфигурация меню и ACL в `root/usr/share/`.

## Сборка

### Через GitHub Actions (рекомендуется)

Используйте workflow "New - Build AmneziaWG from SDK" для быстрой сборки (~20 минут). Требуется указать:
- OpenWRT version (SNAPSHOT или стабильная, например 24.10.2)
- Target/Subtarget
- Architecture (например, aarch64_cortex-a53)

### Локальная сборка с OpenWrt SDK

```bash
# Добавить feeds.conf.default в OpenWrt директорию
echo "src-git awgopenwrt https://github.com/this-username-has-been-taken/amneziawg-openwrt.git" >> feeds.conf.default

# Обновить feeds
./scripts/feeds update -a
./scripts/feeds install -a

# Настроить firmware
make menuconfig  # Выбрать amneziawg-go или kmod-amneziawg + amneziawg-tools + luci-proto-amneziawg

# Собрать
make defconfig
make -j$(nproc) V=sc
```

**Важно:** Для сборки `amneziawg-go` требуется версия Go > 1.24.4. В большинстве стабильных версий OpenWRT Go старее. Необходимо заменить `{openwrt}/feeds/packages/lang/golang` на актуальную версию из репозитория OpenWRT Packages.

## Выбор между kmod-amneziawg и amneziawg-go

- **kmod-amneziawg** (по умолчанию): kernel module, меньше ресурсов, выше производительность
- **amneziawg-go**: userspace реализация, используется если kernel module не работает

Если установлены оба пакета, по умолчанию используется `kmod-amneziawg`.

## Управление версиями

Формат версий: `x.y.z` где `x.y` — версия AmneziaWG, `z` — версия YAAWG.

При обновлении версий апстрим-проектов необходимо обновить:
- `PKG_VERSION`, `PKG_SOURCE_VERSION`, `PKG_MIRROR_HASH` в соответствующих Makefile
- Версию в `luci-proto-amneziawg/Makefile` (`PKG_VERSION`)

## Vermagic (только для старого workflow)

Для SNAPSHOT версий OpenWRT `vermagic` должен совпадать с версией kernel. Проверяется командой `apk info kernel` или `opkg info kernel` (хеш после версии в поле `Version`).

## Зависимости и upstream репозитории

Все upstream зависимости скачиваются автоматически через OpenWrt build system при сборке. Они указаны в Makefiles:
- amneziawg-tools: `PKG_SOURCE_URL=https://github.com/amnezia-vpn/amneziawg-tools.git`
- amneziawg-go: `PKG_SOURCE_URL=https://github.com/amnezia-vpn/amneziawg-go.git`
- kmod-amneziawg: `PKG_SOURCE_URL=https://github.com/amnezia-vpn/amneziawg-linux-kernel-module.git`

При обновлении upstream необходимо также обновить `PKG_MIRROR_HASH` (хеш исходников для проверки целостности).

## Важные скрипты

- `amneziawg-tools/files/amneziawg.sh` — netifd protocol helper, отвечает за создание/удаление интерфейсов, применение конфигурации
- `amneziawg-tools/files/amneziawg_watchdog` — watchdog-скрипт для мониторинга и перезапуска
- `luci-proto-amneziawg/htdocs/luci-static/resources/protocol/amneziawg.js` — JavaScript код для Web UI (LuCI)

## Поддерживаемые параметры протокола AmneziaWG v2.0

- **Jc, Jmin, Jmax** — параметры джамминга
- **S1, S2, S3, S4** — параметры маскировки (S3-S4 добавлены в v2.0)
- **H1, H2, H3, H4** — параметры заголовков (поддерживают ranged значения через дефис: `123456-123500`)
- **I1, I2, I3, I4, I5** — параметры干扰 (I1-I5 добавлены в v2.0)

# ADDCLAUDE.md - Руководство по использованию агентов

## Обзор системы агентов

Этот документ содержит полное руководство по использованию всех специализированных агентов в проекте. Агенты помогают автоматически делегировать задачи подходящим специалистам, обеспечивая качественную разработку.

### Как работают агенты

Агенты запускаются автоматически через инструмент `Task` когда задача соответствует их описанию. Каждый агент:
- Работает в отдельном контексте (не засоряет основной диалог)
- Имеет собственную системную инструкцию
- Может использовать ограниченный набор инструментов
- Возвращает результаты в основной диалог

---

## Справочник агентов

### 🏛️ Leadership & Coordination

#### **tech-lead** (model: opus, color: purple)

**Когда использовать:**
- Планирование сложной архитектуры системы
- Координация нескольких специалистов
- Рефакторинг крупных компонентов
- Управление релизами

**Примеры запросов:**
```
"Спроектируй архитектуру для нашей новой системы авторизации"
"Нам нужно реорганизовать кодовую базу для улучшения масштабируемости"
"Спланируй релиз v2.0 с учётом обратной совместимости"
"Координируй работу между frontend и backend командами"
```

**Запуск через Task:**
```javascript
Task({
  subagent_type: "tech-lead",
  description: "Планирование архитектуры",
  prompt: "Спроектируй архитектуру системы оплаты с учётом масштабируемости"
})
```

---

#### **product-owner** (model: sonnet, color: red)

**Когда использовать:**
- Приоритизация фич в backlog
- Разбивка эпиков на user stories
- Создание критериев приёмки
- RICE/MoSCoW анализ

**Примеры запросов:**
```
"Приоритизируй наш backlog из 10 фич"
"Разбери функцию оплаты на user stories"
"Определи критерии приёмки для новой системы авторизации"
"Сделай RICE анализ для нашего roadmap"
```

---

#### **orchestrator** (model: opus, color: gold)

**Когда использовать:**
- Реализация больших функций (требует несколько специалистов)
- Параллельная разработка (frontend + backend одновременно)
- Координация cross-functional проектов

**Примеры запросов:**
```
"Реализуй полноценную систему электронной коммерции"
"Параллельно работай над API и UI"
"Построй дашборд аналитики с нуля"
"Координируй работу design, frontend, backend и database специалистов"
```

**Важное примечание:** Используйте orchestrator для инициатив, которые требуют:
- Больше 2-3 специалистов
- Нескольких фаз разработки
- Тесной координации между командами

---

### 🎨 Frontend Team

#### **front-end-lead** (model: opus, color: green)

**Когда использовать:**
- Планирование архитектуры UI компонентов
- Оптимизация производительности (Core Web Vitals)
- Выбор frontend технологий
- Проектирование Design System

**Примеры запросов:**
```
"Как нам структурировать новый React проект?"
"Оптимизируй производительность, Core Web Vitals failing"
"Спроектируй Design System для нашего UI"
"Выбери подход для state management в нашем приложении"
```

---

#### **front-end-engineer** (model: sonnet, color: green)

**Когда использовать:**
- Реализация UI компонентов
- Создание новых страниц
- Интеграция с API
- Добавление state management

**Примеры запросов:**
```
"Создай переиспользуемый компонент модального окна"
"Добавь страницу настроек с пользовательскими предпочтениями"
"Подключи frontend к API авторизации"
"Реализуй форму с валидацией"
```

**Примечание:** Используйте front-end-engineer для написания кода. Для архитектурных решений используйте front-end-lead.

---

#### **ui-ux-designer** (model: sonnet, color: pink)

**Когда использовать:**
- Дизайн user flows
- Создание wireframes
- Улучшение UX существующих фич
- Дизайн компонентов

**Примеры запросов:**
```
"Спроектируй onboarding flow для новых пользователей"
"Пользователи путаются в нашей странице настроек"
"Создай дизайн для таблицы данных"
"Улучши UX нашего checkout процесса"
```

---

#### **ai-ux-ui-engineer** (model: sonnet, color: cyan)

**Когда использовать:**
- Создание AI chat интерфейсов
- Дизайн AI-powered UI компонентов
- Отображение AI suggestion
- Feedback механизмы для AI

**Примеры запросов:**
```
"Добавь AI chat интерфейс в приложение"
"Сделай AI suggestions более интерактивными"
"Спроектируй UI для feedback на AI ответы"
"Реализуй интерфейс для AI-powered рекомендаций"
```

---

### ⚙️ Backend Team

#### **back-end-lead** (model: opus, color: blue)

**Когда использовать:**
- Проектирование backend архитектуры
- Проектирование API (REST/GraphQL/gRPC)
- Планирование microservices миграции
- Выбор backend технологий

**Примеры запросов:**
```
"Нам нужно разделить монолит на microservices"
"Как спроектировать REST API для модуля управления пользователями?"
"Спроектируй масштабируемый backend для SaaS платформы"
"Выбери подход для database scaling"
```

---

#### **back-end-engineer** (model: sonnet, color: blue)

**Когда использовать:**
- Реализация API endpoints
- Написание business logic
- Database операции
- Интеграции с external services

**Примеры запросов:**
```
"Создай endpoint для регистрации пользователей"
"Добавь интеграцию со Stripe для платежей"
"Реализуй repository pattern для доступа к данным"
"Обработай webhooks от внешнего сервиса"
```

**Примечание:** Используйте back-end-engineer для написания кода. Для архитектурных решений используйте back-end-lead.

---

### 🗄️ Data & Infrastructure

#### **db-lead** (model: opus, color: indigo)

**Когда использовать:**
- Проектирование database schema
- Оптимизация запросов
- Планирование миграций
- Database scaling стратегии

**Примеры запросов:**
```
"Спроектируй database schema для e-commerce платформы"
"Наши запросы медленные, дашборд грузится 10 секунд"
"Запланируй миграцию базы данных на PostgreSQL"
"Как масштабировать базу данных для 10x роста?"
```

---

#### **dev-ops** (model: sonnet, color: orange)

**Когда использовать:**
- Настройка CI/CD пайплайнов
- Containerization (Docker, Kubernetes)
- Infrastructure as Code
- Monitoring и alerting

**Примеры запросов:**
```
"Настрой CI/CD pipeline для Node.js приложения"
"Dockerize приложение и разверни в Kubernetes"
"Настрой monitoring и alerting для production"
"Создай infrastructure через Terraform"
```

---

#### **integration-lead** (model: opus, color: teal)

**Когда использовать:**
- Проектирование system integrations
- Design API для external integrations
- Webhook системы
- Data synchronization

**Примеры запросов:**
```
"Интегрируй наше приложение с Salesforce"
"Настрой webhooks для real-time updates"
"Спроектируй двунаправленную синхронизацию между системами"
"Создай надёжную систему интеграции с внешним API"
```

---

### 🧪 Quality & Testing

#### **qa-engineer** (model: sonnet, color: yellow)

**Когда использовать:**
- Создание test plans
- Написание automated tests
- Test strategy
- Валидация functionality

**Примеры запросов:**
```
"Создай test plan для функции checkout"
"Напиши E2E тесты для login flow"
"Нам нужна testing strategy для нового API"
"Проверь coverage тестов"
```

---

### 🤖 AI & Analytics

#### **ai-lead** (model: sonnet, color: cyan)

**Когда использовать:**
- Планирование AI/ML архитектуры
- Multi-provider интеграции
- AI feature strategy
- Оптимизация AI производительности

**Примеры запросов:**
```
"Хочу добавить DeepSeek как AI провайдер"
"AI ответы медленные, как оптимизировать?"
"Спроектируй AI-powered систему рекомендаций"
"Выбери стратегию для multi-provider AI архитектуры"
```

---

#### **ai-backend-engineer** (model: sonnet, color: cyan)

**Когда использовать:**
- Реализация AI features в backend
- Интеграция AI провайдеров
- Оптимизация AI сервисов
- AI pipeline реализация

**Примеры запросов:**
```
"Добавь DeepSeek как опцию AI провайдера"
"AI анализ email слишком медленный"
"Реализуй AI-powered summarization текста"
"Оптимизируй AI pipeline"
```

---

#### **analytics** (model: sonnet, color: purple)

**Когда использовать:**
- Анализ user behavior
- A/B test анализ
- Metrics tracking
- Data-driven решения

**Примеры запросов:**
```
"Проанализируй почему пользователи отваливаются на onboarding"
"Проанализируй результаты A/B теста кнопки signup"
"Настрой tracking для наших KPI"
"Создай funnel analysis"
```

---

### 🐛 Debugging & Support

#### **debugger** (model: opus, color: red)

**Когда использовать:**
- Анализ stack traces
- Memory leak диагностика
- Performance profiling
- Сложные баги

**Примеры запросов:**
```
"Приложение падает с ошибкой" [показать stack trace]
"Сервер заканчивается память через несколько часов"
"Приложение замедляется при обработке больших данных"
"Найди причину утечки памяти"
```

**Когда использовать debugger vs tech-support:**
- **debugger**: Сложные технические проблемы, требующие глубокого анализа
- **tech-support**: Простые проблемы, конфигурация, базовая troubleshooting

---

#### **tech-support** (model: haiku, color: red)

**Когда использовать:**
- Диагностика простых ошибок
- Configuration помощь
- Installation проблемы
- User assistance

**Примеры запросов:**
```
"Приложение не запускается, ошибка 'connection refused'"
"Как настроить API settings?"
"Не могу установить зависимости, npm ошибки"
"Помоги с настройкой окружения"
```

---

### 📋 Release & Version Management

#### **version-manager** (model: haiku, color: violet)

**Когда использовать:**
- Определение следующей версии
- Генерация changelog
- Release планирование
- Semver compliance

**Примеры запросов:**
```
"Мы добавили новые фичи и исправили баги, какую версию выпускать?"
"Сгенерируй changelog для предстоящего релиза"
"Запланируй релиз v2.0 с breaking changes"
"Следует ли увеличивать major версию?"
```

---

## Рекомендуемые workflow-ы

### Workflow 1: Новая функция от начала до конца

```
1. product-owner → Определить user stories и приоритеты
2. ui-ux-designer → Спроектировать user flows и UI
3. back-end-lead + front-end-lead → Спроектировать архитектуру
4. back-end-engineer → Реализовать backend API
5. front-end-engineer → Реализовать UI компоненты
6. qa-engineer → Написать и запустить тесты
7. debugger → Починить найденные баги
8. version-manager → Подготовить релиз
```

**Или через orchestrator для координации:**
```
orchestrator → "Реализуй функцию оплаты с нуля"
  (автоматически координирует всех специалистов)
```

---

### Workflow 2: Performance оптимизация

```
1. analytics → Проанализировать метрики и найти проблемы
2. db-lead → Оптимизировать database queries
3. front-end-lead → Оптимизировать frontend performance
4. debugger → Профилировать и найти bottlenecks
```

---

### Workflow 3: Интеграция с внешним сервисом

```
1. integration-lead → Спроектировать архитектуру интеграции
2. back-end-engineer → Реализовать интеграцию
3. qa-engineer → Написать тесты для интеграции
4. debugger → Решить проблемы с интеграцией
```

---

### Workflow 4: AI feature реализация

```
1. ai-lead → Спроектировать AI архитектуру
2. ai-backend-engineer → Реализовать backend AI feature
3. ai-ux-ui-engineer → Создать UI для AI feature
4. qa-engineer → Протестировать AI functionality
```

---

### Workflow 5: Рефакторинг

```
1. tech-lead → Спроектировать архитектуру рефакторинга
2. back-end-lead + front-end-lead → Планировать изменения
3. back-end-engineer + front-end-engineer → Реализовать рефакторинг
4. qa-engineer → Убедиться что ничего не сломалось
5. debugger → Починить regression bugs
```

---

### Workflow 6: Подготовка релиза

```
1. version-manager → Определить версию и создать changelog
2. qa-engineer → Запустить полное тестирование
3. tech-lead → Координировать релиз
4. dev-ops → Развернуть в production
```

---

## Правила выбора агента

### Выберите **Lead** агента когда:
- ✅ Нужно architectural решение
- ✅ Планируется сложная система
- ✅ Требуется координация команды
- ✅ Нужна оценка trade-offs

### Выберите **Engineer** агента когда:
- ✅ Нужно написать код
- ✅ Реализовать конкретную функцию
- ✅ Модифицировать существующий код
- ✅ Создать компонент или endpoint

### Выберите **orchestrator** когда:
- ✅ Инициатива требует 3+ специалистов
- ✅ Нужна параллельная работа
- ✅ Сложная cross-functional задача
- ✅ Несколько фаз разработки

### Выберите **debugger** когда:
- ✅ Сложный stack trace
- ✅ Memory leak
- ✅ Performance issue
- ✅ Неясная причина проблемы

### Выберите **tech-support** когда:
- ✅ Простая ошибка с понятным сообщением
- ✅ Configuration вопрос
- ✅ Installation проблема
- ✅ Базовая troubleshooting

---

## Советы по эффективному использованию

### 1. Будьте конкретны в запросах

❌ **Плохо:** "Почини баг"
✅ **Хорошо:** "Приложение падает с NullPointerException при попытке войти"

### 2. Указывайте контекст

❌ **Плохо:** "Создай API"
✅ **Хорошо:** "Создай REST API для управления пользователями с CRUD операциями"

### 3. Используйте правильный уровень агента

❌ **Плохо:** Использовать front-end-engineer для проектирования архитектуры
✅ **Хорошо:** Использовать front-end-lead для архитектуры, затем front-end-engineer для реализации

### 4. Делегируйте соответствующие задачи

- Для **architectural решений** → используйте Lead агентов
- Для **написания кода** → используйте Engineer агентов
- Для **координации** → используйте orchestrator
- Для **багов** → используйте debugger или tech-support

---

## Примеры сценариев

### Сценарий 1: Пользователь хочет добавить новую фичу

**Запрос:** "Хочу добавить social login"

**Правильный подход:**
1. Использовать **product-owner** для определения требований
2. Использовать **back-end-lead** для проектирования backend
3. Использовать **front-end-lead** для проектирования UI
4. Использовать **back-end-engineer** для реализации
5. Использовать **front-end-engineer** для UI
6. Использовать **qa-engineer** для тестирования

**Или через orchestrator:**
```
"Используй orchestrator для реализации social login с нуля"
```

---

### Сценарий 2: Медленная база данных

**Запрос:** "Queries медленные, dashboard грузится 10 секунд"

**Правильный подход:**
1. Использовать **db-lead** для анализа и оптимизации
2. Использовать **debugger** если нужно профилирование
3. Использовать **back-end-engineer** для реализации оптимизаций

---

### Сценарий 3: Приложение падает

**Запрос:** "App crashes with stack trace"

**Правильный подход:**
- Если stack trace сложный → **debugger**
- Если простая ошибка типа "connection refused" → **tech-support**

---

### Сценарий 4: Релиз новой версии

**Запрос:** "Готовим v2.0 релиз"

**Правильный подход:**
1. **version-manager** → определить версию и changelog
2. **qa-engineer** → полное тестирование
3. **tech-lead** → координация релиза
4. **dev-ops** → deployment

---

## Заключение

Эта система агентов создана для автоматического делегирования задач подходящим специалистам. Ключевые принципы:

1. **Правильный агент для правильной задачи**
2. **Lead для архитектуры, Engineer для реализации**
3. **Orchestrator для сложных multi-team инициатив**
4. **Debugger для сложных проблем, tech-support для простых**

Следуя этим принципам, вы обеспечите качественную разработку и эффективное использование агентов.

